---
import MainLayout from '../layouts/MainLayout.astro';
import '../styles/settings/skills.css';

// Get the current path for active link highlighting
const currentPath = Astro.url.pathname;

// Get the content and headings
const content = await Astro.glob('../content/skills.md');
const pageContent = content[0];
const headings = pageContent.getHeadings();
---

<MainLayout title="Skills - Portfolio" headings={headings} currentPath={currentPath}>
  <link rel="stylesheet" href="/styles/settings/skills.css" slot="head" />
  <pageContent.Content />
</MainLayout>
